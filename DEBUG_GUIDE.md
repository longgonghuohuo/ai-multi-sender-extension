# 🔍 调试指南 - 为什么显示"发送到10个AI"

## 📋 测试步骤

### 第一步：刷新插件
```bash
1. chrome://extensions/
2. 找到 AI Multi Sender
3. 点击刷新 🔄
4. 确保插件已启用
```

### 第二步：打开控制台
```bash
1. 点击插件图标（打开 simple.html 页面）
2. 按 F12 打开开发者工具
3. 切换到 Console（控制台）标签
4. 清空控制台（点击 🚫 图标）
```

### 第三步：测试"打开所有AI"按钮
```bash
1. 在插件页面点击 "📂 打开所有AI" 按钮
2. 查看控制台输出
3. 应该看到：
   - 📂 正在打开所有 AI...
   - 三个新标签页被打开
4. 等待 3 秒让页面加载
```

### 第四步：检查打开了哪些标签页
```bash
1. 在控制台输入并执行：
   chrome.tabs.query({}, (tabs) => {
     const aiTabs = tabs.filter(t =>
       t.url && (
         t.url.includes('chatgpt.com') ||
         t.url.includes('claude.ai') ||
         t.url.includes('gemini.google.com')
       )
     );
     console.log('AI 标签页数量:', aiTabs.length);
     console.log('AI 标签页列表:', aiTabs.map(t => t.url));
   });

2. 这会告诉你：
   - 有多少个 AI 标签页
   - 每个标签页的 URL
```

### 第五步：测试发送功能
```bash
1. 在输入框输入测试问题："你好"
2. 点击 "🚀 发送到所有AI" 按钮
3. 观察控制台输出，应该看到：

[调试] sendToAll 函数被调用了！
[调试] 输入的问题: 你好
[发送] 开始发送: 你好
[发送] 找到 X 个AI标签页
[发送] 处理标签页: https://chatgpt.com/...
[发送] 成功: https://chatgpt.com/...
[发送] 处理标签页: https://claude.ai/...
[发送] 成功: https://claude.ai/...
[发送] 处理标签页: https://gemini.google.com/...
[发送] 成功: https://gemini.google.com/...
[发送] 最终结果统计:
  - 成功: 3 个
  - 失败: 0 个
  - 成功列表: [...]
  - 失败列表: []
[发送] 显示成功消息: ✅ 成功发送到 3 个 AI
```

## 🐛 可能的问题

### 问题1: 显示"发送到10个AI"

**原因分析：**
- 可能你的浏览器打开了多个 ChatGPT/Claude/Gemini 标签页
- 每个标签页都会被计数

**验证方法：**
```bash
1. 在控制台输入：
   chrome.tabs.query({}, (tabs) => {
     tabs.forEach(t => {
       if (t.url && (
         t.url.includes('chatgpt.com') ||
         t.url.includes('claude.ai') ||
         t.url.includes('gemini.google.com')
       )) {
         console.log('找到AI标签页:', t.url);
       }
     });
   });

2. 查看输出，数一下有多少个
```

**解决方法：**
- 关闭多余的 AI 标签页
- 每个 AI 只保留一个标签页

### 问题2: 按钮点击没反应

**可能原因：**
1. simple.js 没有正确加载
2. 函数没有绑定到按钮
3. JavaScript 错误

**验证方法：**
```bash
1. 打开控制台（F12）
2. 查看是否有红色错误信息
3. 在控制台输入: typeof sendToAll
   - 如果显示 "function" = 正常
   - 如果显示 "undefined" = 函数没加载
4. 手动调用: sendToAll()
   - 看是否执行
```

### 问题3: 没有自动点击发送按钮

**原因：**
- AI 页面的发送按钮选择器可能变化了
- 需要等待时间不够

**验证方法：**
```bash
1. 切换到 ChatGPT 标签页
2. 按 F12 打开控制台
3. 查看是否有日志：[AI页面] 点击发送按钮: ...
4. 检查输入框是否有内容
5. 手动检查发送按钮是否被点击
```

## 🔧 快速修复命令

### 检查当前AI标签页数量
```javascript
chrome.tabs.query({}, (tabs) => {
  const aiTabs = tabs.filter(t =>
    t.url && (
      t.url.includes('chatgpt.com') ||
      t.url.includes('claude.ai') ||
      t.url.includes('gemini.google.com')
    )
  );
  console.log('========== AI 标签页统计 ==========');
  console.log('总数:', aiTabs.length);
  aiTabs.forEach((t, i) => {
    console.log(`${i + 1}. ${t.url}`);
  });
  console.log('===================================');
});
```

### 关闭多余的AI标签页（只保留每个AI一个）
```javascript
chrome.tabs.query({}, (tabs) => {
  const seen = { chatgpt: false, claude: false, gemini: false };
  tabs.forEach(t => {
    if (!t.url) return;

    if (t.url.includes('chatgpt.com')) {
      if (seen.chatgpt) chrome.tabs.remove(t.id);
      else seen.chatgpt = true;
    } else if (t.url.includes('claude.ai')) {
      if (seen.claude) chrome.tabs.remove(t.id);
      else seen.claude = true;
    } else if (t.url.includes('gemini.google.com')) {
      if (seen.gemini) chrome.tabs.remove(t.id);
      else seen.gemini = true;
    }
  });
  console.log('已清理重复的AI标签页');
});
```

## 📊 预期的正常输出

当一切正常时，点击发送按钮后，控制台应该显示：

```
[调试] sendToAll 函数被调用了！
[调试] 输入的问题: 你好
[发送] 开始发送: 你好
[发送] 找到 3 个AI标签页
[发送] 处理标签页: https://chatgpt.com/c/xxxxx
[发送] 成功: https://chatgpt.com/c/xxxxx
[发送] 处理标签页: https://claude.ai/chat/xxxxx
[发送] 成功: https://claude.ai/chat/xxxxx
[发送] 处理标签页: https://gemini.google.com/app
[发送] 成功: https://gemini.google.com/app
[发送] 最终结果统计:
  - 成功: 3 个
  - 失败: 0 个
  - 成功列表: (3) ["https://chatgpt.com/...", "https://claude.ai/...", "https://gemini.google.com/..."]
  - 失败列表: []
[发送] 显示成功消息: ✅ 成功发送到 3 个 AI
```

然后页面顶部显示绿色提示：**✅ 成功发送到 3 个 AI**

---

## 🎯 现在请按照这个步骤测试

1. **刷新插件**
2. **打开插件页面并打开控制台（F12）**
3. **点击"打开所有AI"按钮**
4. **运行检查命令查看AI标签页数量**
5. **输入问题并点击发送**
6. **截图控制台输出发给我**

这样我就能知道具体是哪里出了问题！
