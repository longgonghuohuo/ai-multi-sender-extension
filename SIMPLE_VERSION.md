# ✨ 极简版 - AI Multi Sender

## 🎯 最新改进

### 用户反馈修复
1. ✅ **移除状态检测卡片** - 不再显示三个AI的状态检测
2. ✅ **极简界面** - 只有输入框和按钮
3. ✅ **自动发送** - 填充后自动点击AI页面的发送按钮
4. ✅ **精确筛选** - 只处理3个AI网站，不会误发送到其他标签页

## 🚀 使用方法

### 第一步：刷新插件
```bash
1. chrome://extensions/
2. 找到 AI Multi Sender
3. 点击刷新 🔄
```

### 第二步：打开AI网站
```bash
两种方式：

方式1（自动）：
1. 点击插件图标
2. 点击"📂 打开所有AI"按钮
3. 自动打开 ChatGPT、Claude、Gemini

方式2（手动）：
1. 手动打开 https://chatgpt.com
2. 手动打开 https://claude.ai
3. 手动打开 https://gemini.google.com
```

### 第三步：发送问题
```bash
1. 在插件页面输入问题
2. 点击"🚀 发送到所有AI"（或按 Ctrl+Enter）
3. 自动填充并发送到三个AI
4. 切换到AI标签页查看回答
```

## 📺 界面预览

```
┌────────────────────────────────────────┐
│                                        │
│     🤖 AI Multi Sender                │
│     一键向多个AI发送相同问题           │
│                                        │
│     💬 输入你的问题                    │
│     ┌────────────────────────────┐   │
│     │                            │   │
│     │  在这里输入问题...          │   │
│     │                            │   │
│     └────────────────────────────┘   │
│                                        │
│  [📂 打开所有AI] [🚀 发送] [🗑️ 清空] │
│                                        │
│  快捷键: Ctrl + Enter 发送            │
│                                        │
└────────────────────────────────────────┘
```

## ✨ 特点

1. **极简设计**
   - 没有复杂的状态检测
   - 没有绿色/红色指示器
   - 只有输入和发送

2. **自动化**
   - 自动填充输入框
   - 自动点击发送按钮
   - 无需手动操作AI页面

3. **智能筛选**
   - 只处理3个AI网站
   - 不会发送到其他标签页
   - 精确匹配域名

4. **体验优化**
   - 快捷键支持 (Ctrl+Enter)
   - 自动保存输入内容
   - Toast提示反馈

## 🔧 技术实现

### 关键改进

1. **严格筛选标签页**
```javascript
const targetAITabs = tabs.filter(tab => {
  if (!tab.url) return false;
  return (
    tab.url.includes('chatgpt.com') ||
    tab.url.includes('claude.ai') ||
    tab.url.includes('gemini.google.com')
  );
});
```

2. **自动点击发送按钮**
```javascript
setTimeout(() => {
  const sendButtonSelectors = [
    'button[data-testid="send-button"]',      // ChatGPT
    'button[aria-label="Send Message"]',      // Claude
    'button[aria-label="Send message"]'       // Gemini
  ];

  for (const btnSelector of sendButtonSelectors) {
    const sendBtn = document.querySelector(btnSelector);
    if (sendBtn && !sendBtn.disabled) {
      sendBtn.click();
      break;
    }
  }
}, 300);
```

## ⚠️ 注意事项

1. **必须先打开AI网站**
   - 使用前确保三个AI网站已打开
   - 可以用"打开所有AI"按钮自动打开

2. **确保在对话页面**
   - ChatGPT: 在 chatgpt.com 的对话页面
   - Claude: 在 claude.ai 的对话页面
   - Gemini: 在 gemini.google.com 主页

3. **每次更新后刷新插件**
   - chrome://extensions/
   - 点击刷新按钮

## 🎊 完成的功能

- ✅ 极简界面（无状态检测）
- ✅ 一键打开所有AI
- ✅ 自动填充输入框
- ✅ 自动点击发送按钮
- ✅ 只处理3个AI网站
- ✅ 快捷键支持
- ✅ 自动保存输入

## 📋 下一步可能的改进

1. 图片/文件上传支持
2. AI之间的讨论功能
3. 历史记录查看
4. 自定义AI网站列表

---

**立即刷新插件并开始使用！** 🚀
